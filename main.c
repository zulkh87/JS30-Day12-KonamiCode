#include <stdio.h>
#include <stdlib.h>

int main()
{
    char characterName[] = "yoso";
    int characterAge = 32;
    printf("There was once a man named %s\n", characterName);
    printf("He was %d years old", characterAge);
    return 0;
}
